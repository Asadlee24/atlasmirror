# LP-0018 Compliance & Verification Matrix

This matrix maps every authoritative requirement from [Logos λPrize LP-0018](https://github.com/logos-co/lambda-prize/blob/master/prizes/LP-0018.md) to its implementation, testing, evidence, and status.

| # | Requirement | Implementation | Test | Evidence | Status |
|---|---|---|---|---|---|
| **F1** | **Region discovery**: Fetch Geofabrik index, display available regions with version info and tree level (`country` / `subregion`), restricted to predefined closed set. | `atlasmirror-sdk/src/geofabrik_adapter.cpp`, `atlasmirror-cli/src/commands/regions.rs`, `metadata/regions.json` | `tests/geofabrik_test.rs`, `atlasmirror-sdk/tests/test_geofabrik.cpp` | CLI `atlasmirror regions list`, Basecamp Regions table, unit test logs | IN PROGRESS |
| **F2** | **Host workflow**: Download PBF from Geofabrik → store to Logos Storage → register on-chain via OSM registry LEZ program. | `atlasmirror-sdk/src/host_pipeline.cpp`, `atlasmirror-cli/src/commands/host.rs` | `scripts/demo.sh`, `tests/e2e_host_test.rs` | Local sequencer TX hash, CID output, demo log | IN PROGRESS |
| **F3** | **Download workflow**: Fetch from Logos Storage (if hosted by CID) or Geofabrik (direct fallback). Content-addressed integrity; no round-trip to Geofabrik to trust downloaded file. | `atlasmirror-sdk/src/download_pipeline.cpp`, `atlasmirror-cli/src/commands/download.rs` | `tests/download_test.rs` | CLI `atlasmirror download`, SHA256/CID verification log | IN PROGRESS |
| **F4** | **Bulk host**: Allow selecting multiple regions to host at once with per-region opt-out. Strictly bounded to non-overlapping predefined set; warn if overlap attempted. | `atlasmirror-sdk/src/bulk_host.cpp`, `atlasmirror-app/src/qml/HostingQueueView.qml` | `tests/bulk_host_test.rs` | Bulk host queue UI, CLI `atlasmirror host --many` | IN PROGRESS |
| **F5** | **Local import**: User provides local PBF → verify against Geofabrik published MD5 (import-time only) → store → register by CID. | `atlasmirror-sdk/src/local_import.cpp`, `atlasmirror-cli/src/commands/host.rs` (`--file`) | `tests/local_import_test.rs` | CLI `atlasmirror host --file`, UI file picker verification screen | IN PROGRESS |
| **F6** | **Update check**: Compare central Geofabrik versions vs registry and notify user of available updates. Tracked per region entry independently. | `atlasmirror-sdk/src/update_checker.cpp`, `atlasmirror-cli/src/commands/updates.rs` | `tests/update_checker_test.rs` | CLI `atlasmirror updates`, UI `Update available` badges | IN PROGRESS |
| **F7** | **On-chain registry**: SPEL LEZ program storing `region`, `parent`, `level`, `cid`, `source_url`, `checksum`, `version`, `hosted`, `timestamp`; unique Geofabrik path; queryable by region, parent, CID; batch registration. | `osm-registry/methods/guest/src/bin/osm_registry.rs`, `osm-registry/registry_core/src/lib.rs` | `osm-registry/tests/registry_tests.rs` | SPEL IDL (`osm-registry/idl/osm_registry.json`), LEZ TX receipts | IN PROGRESS |
| **F8** | **Testnet 0.3**: OSM registry operates end-to-end against Logos LEZ zone on Logos testnet 0.3. | `scripts/testnet-deploy.sh`, `docs/testnet.md` | `scripts/testnet-smoke.sh` | Deployed Program ID, on-chain state queries on testnet 0.3 | IN PROGRESS |
| **F9** | **OSM registry SDK**: Self-contained Basecamp SDK/module (`mkLogosModule`), stable documented API (`atlasmirror_sdk_impl.h`), minimal embedding example in another Basecamp module. | `atlasmirror-sdk/`, `examples/consumer-module/` | `examples/consumer-module/tests/` | `docs/sdk.md`, consumer module resolving `asia/pakistan` -> CID | IN PROGRESS |
| **U1** | **Basecamp App GUI**: Native Basecamp app with local build instructions, downloadable assets, loadable in Logos Basecamp as `.lgx`. | `atlasmirror-app/` (`type: ui_qml`, `mkLogosQmlModule`) | `nix build .#lgx`, Basecamp module loader | Screenshots, `atlasmirror-app.lgx` artifact, video demo | IN PROGRESS |
| **U2** | **CLI Tool**: Covering host, batch-register, lookup by region/parent/CID, update check, download, local import, and `doctor`. | `atlasmirror-cli/src/main.rs` | `tests/cli_tests.rs` | CLI manpage/help output, test runs, JSON mode output | IN PROGRESS |
| **U3** | **Clear UX**: Show hosted/not-hosted status, version numbers, verification results, sober Logos styling (#000000 / #FFFFFF). | `atlasmirror-app/src/qml/` | Visual inspection, UI tests | High contrast, clear state machine, WCAG contrast compliant | IN PROGRESS |
| **R1** | **Storage upload retry & backoff**: Exponential backoff on transient failures, clear terminal error after exhausting retries. | `atlasmirror-sdk/src/storage_adapter.cpp` | `tests/storage_retry_test.rs` | Retry test logs with backoff timestamps | IN PROGRESS |
| **R2** | **Import-time checksum verification**: Graceful failure with clear error messages on checksum mismatch; never store corrupted/tampered PBF. | `atlasmirror-sdk/src/checksum.cpp` | `tests/checksum_test.rs` | Tampered file test passing with `CHECKSUM_MISMATCH` | IN PROGRESS |
| **R3** | **Timestamp ordering**: Registry entries ordered by timestamp for deterministic version sorting. | `osm-registry/methods/guest/src/bin/osm_registry.rs` | `osm-registry/tests/timestamp_sort_test.rs` | Unit test confirming chronological sorting | IN PROGRESS |
| **R4** | **No mandatory centralised services**: No reliance on third-party web services beyond configured sequencer, Logos LEZ node, Logos Storage, and Geofabrik as snapshot source. Zero telemetry by default. | `atlasmirror-sdk/src/config.cpp` | Network traffic inspection | Fully functional with external web blocked (offline localnet mode) | IN PROGRESS |
| **P1** | **Cycle Count Benchmarking**: Document cycle count usage for each on-chain operation (`register`, `batch-register`) using LEZ `cycle_bench`. | `scripts/bench-cu.sh`, `docs/performance.md` | `cycle_bench` execution | Real cycle counts recorded in `docs/performance.md` | IN PROGRESS |
| **S1** | **Multi-platform build**: Builds and runs end-to-end on both macOS (Apple Silicon) and Linux (x86_64). WSL2 documented. | `flake.nix`, `.github/workflows/ci.yml` | GitHub Actions runners (`ubuntu-latest`, `macos-14`) | Green CI build logs for both platforms | IN PROGRESS |
| **S2** | **logos-module-builder**: Basecamp modules built with `mkLogosModule` and `mkLogosQmlModule`. | `atlasmirror-sdk/flake.nix`, `atlasmirror-app/flake.nix` | `nix build .#lib`, `nix build .#lgx` | Generated `.lgx` bundles | IN PROGRESS |
| **S3** | **Module catalog publication**: Fork of `logos-modules-release-base`, releases via `logos-modules-release-action`, `logos-repo.json` URL provided. | `logos-modules-release/`, `docs/adoption.md` | Package manager installation | Working `logos-repo.json` endpoint | IN PROGRESS |
| **S4** | **SPEL IDL**: Generated via SPEL framework and committed to `osm-registry/idl/`. | `osm-registry/examples/src/bin/generate_idl.rs` | `osm-registry/idl/osm_registry.json` | Valid committed JSON IDL | IN PROGRESS |
| **S5** | **E2E Integration Tests**: Host (fetch → store → register) → query → download against real local sequencer in CI. | `scripts/demo.sh`, `.github/workflows/e2e.yml` | `scripts/demo.sh` run in CI | Green E2E pipeline run with full log | IN PROGRESS |
| **S6** | **Authoritative README**: Setup, deployed program ID, region list, CLI + app step-by-step usage, FURPS assessment. | `README.md` | Documentation review | Complete documentation matching evaluator requirements | IN PROGRESS |
| **A1** | **Coverage (Adoption)**: At least 15 countries and 25 total verified region entries hosted in Logos Storage and registered on testnet 0.3. | `metadata/regions.json`, `docs/adoption.md` | Hash verification against Geofabrik MD5 | On-chain registry state + storage CID list matching Geofabrik | IN PROGRESS |
| **A2** | **Ecosystem Reuse (Adoption)**: At least 5 independent modules (including at least 3 Logos Basecamp apps) consuming the OSM module. | `docs/adoption.md`, `examples/consumer-module/` | Code inspection of independent forges | 5 public repository links with genuine commit history | IN PROGRESS |
